<Window x:Class="SerialDebugAssistant.Views.Dialogs.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="发现新版本"
        Width="460" Height="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        ShowInTaskbar="False">

    <Border Background="#1F1F1F" BorderBrush="#2B2B2B" BorderThickness="1" CornerRadius="8">
        <Grid Margin="28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
                <TextBlock Text="🔄" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="发现新版本" FontSize="18" FontWeight="Bold" Foreground="#CCCCCC" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- 版本号 -->
            <TextBlock Grid.Row="1" x:Name="VersionBlock" FontSize="14" Foreground="#4EC9B0" Margin="0,8,0,16"/>

            <!-- 更新说明 -->
            <Border Grid.Row="2" Background="#2D2D2D" CornerRadius="6" Padding="14">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="ReleaseNotesBlock" FontSize="13" Foreground="#B0B0B0"
                               TextWrapping="Wrap" LineHeight="22"/>
                </ScrollViewer>
            </Border>

            <!-- 按钮 -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="LaterButton" Content="稍后" Width="90" Height="34"
                        Foreground="#999999" Background="#333333" BorderThickness="0"
                        FontSize="13" Cursor="Hand"
                        Click="LaterButton_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button x:Name="UpdateButton" Content="立即更新" Width="110" Height="34" Margin="12,0,0,0"
                        Foreground="White" Background="#0078D4" BorderThickness="0"
                        FontSize="13" FontWeight="SemiBold" Cursor="Hand"
                        Click="UpdateButton_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>

</Window>